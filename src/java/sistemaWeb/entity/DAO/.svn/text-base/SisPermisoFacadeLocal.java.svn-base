/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package sistemaWeb.entity.DAO;

import java.util.List;
import javax.ejb.Local;
import sistemaWeb.entity.SisPermiso;

/**
 *
 * @author developer1
 */
@Local
public interface SisPermisoFacadeLocal {

    void create(SisPermiso sisPermiso);

    void edit(SisPermiso sisPermiso);

    void remove(SisPermiso sisPermiso);

    SisPermiso find(Object id);

    List<SisPermiso> findAll();

    List<SisPermiso> findRange(int[] range);

    int count();
    
}
