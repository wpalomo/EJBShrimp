/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package produccion.entity.DAO;

import java.util.List;
import javax.ejb.Local;
import produccion.entity.PrdGrameaje;

/**
 *
 * @author developer1
 */
@Local
public interface PrdGrameajeFacadeLocal {

    void create(PrdGrameaje prdGrameaje);

    void edit(PrdGrameaje prdGrameaje);

    void remove(PrdGrameaje prdGrameaje);

    PrdGrameaje find(Object id);

    List<PrdGrameaje> findAll();

    List<PrdGrameaje> findRange(int[] range);

    int count();
    
}
