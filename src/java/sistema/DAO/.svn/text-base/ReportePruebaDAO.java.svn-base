/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */

package sistema.DAO;

import java.sql.Connection;
import java.sql.SQLException;
import java.util.Map;
import javax.annotation.Resource;
import javax.ejb.Stateless;
//import net.sf.jasperreports.engine.JRException;
//import net.sf.jasperreports.engine.JasperFillManager;
//import net.sf.jasperreports.engine.JasperPrint;
//import net.sf.jasperreports.engine.JasperReport;
//import net.sf.jasperreports.engine.util.JRLoader;

/**
 *
 * @author jack
 */
@Stateless
public class ReportePruebaDAO implements ReportePruebaDAOLocal {
    
    // Add business logic below. (Right-click in editor and choose
    // "Insert Code > Add Business Method")

    @Resource (name = "jdbc/conexionTodocompu")
    private javax.sql.DataSource ds;

//    @Override
//    public JasperPrint generarReportePrueba(String reporte, Map parametros) throws SQLException, JRException {
//        Connection cn = ds.getConnection();
//
//        //*System.out.println("encontro conexion " + ds.getConnection());
//
////        //*System.out.println("directorio: " + getClass().getResource("/sistema/reporte/").getPath().toString());
//
//        parametros.put("SUBREPORT_DIR",getClass().getResource("/sistema/reporte/").getPath());
//        JasperReport report = (JasperReport) JRLoader.loadObject(getClass().getResource("/sistema/reporte/" + "reportComprobanteContableAnticipos.jasper"));
//        System.setProperty("java.awt.headless", "true");
////        JasperReport report = (JasperReport) JRLoader.loadObject(getClass().getResource("/sistema/reporte/" + "reportePruebaManual1.jasper"));
//
//        return JasperFillManager.fillReport( report, parametros, cn );
//
//    }

 
}
